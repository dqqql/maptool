# 世界舆图 · TRPG 世界整理工具

> 面向 GM 的本地优先 TRPG 世界整理工具。
> 在羊皮纸无限画布上组织地形、聚落、事件、关系和自由文本备注。

世界舆图不是精细绘图软件，而是一张可以持续扩展的世界关系图：从素材库取出标记，放到画布上，补充说明并建立连线，最后以单个 JSON 文件备份或迁移。

## 功能

- **世界管理**：创建、进入和删除世界，支持导入与导出 `*.world.json` 存档。
- **无限画布**：鼠标锚点缩放、空白处拖拽平移、辅助网格和视图复位。
- **内置素材库**：地形自然、水域交通、聚落建筑、事件标记与通用组件五大类，共 250 个手绘素材。
- **快捷素材**：每个分类可自定义 5 个快捷槽位，配置保存在浏览器本地。
- **用户素材**：上传 PNG、JPG、WEBP 或 SVG，可重命名；仍被节点引用的素材不会被误删。
- **地图节点**：拖放或双击创建，支持移动、缩放、旋转、复制、删除和层级调整；节点放大后名称字号会相应增大。
- **节点属性**：名称、描述及自定义属性，支持文本、多行文本、复选、下拉、数字、星级和进度格。
- **关系连线**：依次选择两个节点建立连线，可填写名称和描述，节点移动时连线自动跟随。
- **文本备注**：初始为单行，按回车增加行数，外框始终随内容等高；可调字号、背景和宽度。
- **优美导出**：一键导出高清 PNG（按全图自动取景或导出当前视图），或导出自包含的离线只读 HTML——他人双击即可缩放、平移、悬浮查看节点属性，但无法编辑。
- **本地存储**：世界、画布和用户素材均保存在 IndexedDB，不上传服务器。

## 文本 Markdown

文本备注支持以下轻量语法：

```md
## 二级标题
普通文字中的 **粗体**
> 引用内容
- 无序列表
1. 有序列表
```

仅解析以上五种格式，其余字符按普通文本显示。调整文本框宽度或字号时，换行和高度会自动重新计算。

## 编辑器操作

| 操作 | 说明 |
| --- | --- |
| 选择 | 点选、拖动或缩放节点和文本；在空白处拖拽平移画布 |
| 连线 | 依次点选两个节点建立关系 |
| 文本 | 在空白处点击创建文本备注，双击文本框直接编辑 |
| 素材拖放 | 从左侧快捷栏拖入画布；双击素材可放到当前视图中心 |
| 完整素材库 | 点击分类中的“更多”，搜索素材或自定义快捷槽位 |
| 导出 | 「导出」菜单：高清图片（全图 / 当前视图）、只读 HTML、或 `*.world.json` 存档 |

快捷键：

- `Delete` / `Backspace`：删除选中内容
- `Ctrl/⌘+C`、`Ctrl/⌘+V`：复制、粘贴节点
- `Ctrl/⌘+D`：快速复制节点
- `Esc`：取消放置、退出当前工具或清除选择

## 技术栈

- Vite、React、TypeScript
- Konva、react-konva
- Zustand
- IndexedDB、idb
- react-router-dom

## 快速开始

需要 Node.js 18 或更高版本。项目字体通过 Google Fonts 加载，完整显示字体需要联网。

```bash
npm install
npm run dev
npm run build
npm run preview
```

开发服务器默认运行在 `http://localhost:5173`。生产构建分两步（已由 `npm run build` 串联）：先构建应用本体，再用 `vite.viewer.config.ts` 把只读查看器打成自包含模板。两者一同输出到 `dist/`：

- `dist/index.html` —— 应用本体。
- `dist/viewer.html` —— 「只读 HTML 导出」所用的自包含模板（内联 React/Konva 与渲染器，仅留一个数据占位符）。导出时由 `src/export/exportHtml.ts` 取同域的该模板并注入世界数据。

> 部署到静态托管（如 Cloudflare Pages）时，构建命令为 `npm run build`、输出目录为 `dist`，`viewer.html` 会随主站同目录发布。应用使用 HashRouter，无需额外的 SPA 重写规则。「只读 HTML 导出」需在已部署/预览的在线环境下使用（dev 源码态下模板尚未内联）。

## 目录结构

```text
src/
  assets/             内置 SVG 素材及通用组件
  canvas/             Konva 画布、节点、连线、文本与 Markdown 排版
  components/         通用 UI 组件（导出菜单等）
  db/                 IndexedDB schema 和 CRUD
  export/             高清 PNG 与只读 HTML 导出
  panels/             素材库、属性面板及弹窗
  routes/             世界首页与编辑器页面
  serialization/      世界 JSON 导入导出
  store/              Zustand 世界与素材状态
  styles/             全局设计系统和羊皮纸主题
  viewer/             只读查看器（自包含 HTML 入口）
  App.tsx             应用路由
  types.ts            领域模型
```

## 数据与兼容性

浏览器数据库名为 `trpg-maptool`。清除站点数据会删除本地世界，因此重要内容应定期导出。

- 导出文件会内嵌当前世界使用的用户素材。
- 内置素材不重复写入导出文件，通过稳定 ID 引用。
- 新版仍可解析素材库重做前使用的旧内置素材 ID。

## 验证

```bash
npm run build
```

仓库内的 `scripts/shot.mjs` 主要用于 WSL Ubuntu 环境下的完整截图流程：

```bash
npx playwright install chromium
npm run dev
node scripts/shot.mjs
```

截图输出到已忽略的 `screenshots/` 目录。

更早的阶段记录见 [`进度文档.md`](./进度文档.md) 与 [`开发计划.md`](./开发计划.md)。
